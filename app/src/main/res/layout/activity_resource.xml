<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="mz.org.csaude.mentoring.viewmodel.resource.ResourceVM" />
        <import type="android.view.View" />
        <import type="mz.org.csaude.mentoring.util.Utilities" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:context=".view.resource.ResourceActivity">

        <!-- TOP BAR: toolbar + M3 SearchBar -->
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fitsSystemWindows="true"
            app:liftOnScroll="true">

            <include
                android:id="@+id/toolbar"
                layout="@layout/app_toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize" />


            <com.google.android.material.search.SearchBar
                android:id="@+id/searchBar"
                style="@style/Widget.Material3.SearchBar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginTop="12dp"
                android:hint="@string/search"
                app:menu="@menu/menu_resource_search_bar"
                app:navigationIcon="@drawable/ic_search" />
        </com.google.android.material.appbar.AppBarLayout>

        <!-- M3 SearchView overlay, ANCHORED to the SearchBar -->
        <com.google.android.material.search.SearchView
            android:id="@+id/searchView"
            style="@style/Widget.Material3.SearchView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:elevation="16dp"
            app:layout_anchor="@id/searchBar">

            <!-- Optional header area inside the overlay: Filter chips -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp">

                <com.google.android.material.chip.ChipGroup
                    android:id="@+id/chipFilters"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:singleLine="false"
                    app:selectionRequired="false">

                    <com.google.android.material.chip.Chip
                        android:id="@+id/chip_hiv"
                        style="@style/Widget.Material3.Chip.Filter"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:checkable="true"
                        android:text="@string/hiv"
                        app:chipBackgroundColor="@color/colorPrimary"
                        android:checked='@{viewModel.hivChecked}'
                        android:onClick='@{() -> viewModel.changeHivChecked()}' />

                    <com.google.android.material.chip.Chip
                        android:id="@+id/chip_tb"
                        style="@style/Widget.Material3.Chip.Filter"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:checkable="true"
                        app:chipBackgroundColor="@color/colorPrimary"
                        android:text="@string/tb"
                        android:checked='@{viewModel.tbChecked}'
                        android:onClick='@{() -> viewModel.changeTBChecked()}' />
                </com.google.android.material.chip.ChipGroup>

            </LinearLayout>
        </com.google.android.material.search.SearchView>

        <!-- CONTENT CARD with RecyclerView -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/rcvCard"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:visibility='@{Utilities.listHasElements(viewModel.nodeList) ? View.VISIBLE : View.GONE}'
            app:cardCornerRadius="16dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp"
            app:cardPreventCornerOverlap="false"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rcv_resources"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:overScrollMode="never"
                android:scrollbars="none"
                android:layoutAnimation="@anim/layout_pop_in_bottom" />
        </com.google.android.material.card.MaterialCardView>

        <!-- EMPTY STATE -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/empty_state"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/sem_registos"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            android:layout_gravity="center"
            android:visibility='@{Utilities.listHasElements(viewModel.nodeList) ? View.GONE : View.VISIBLE}' />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
